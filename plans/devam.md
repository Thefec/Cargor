# ⏭️ DEVAM — Hızlı Oturum Logu

> **Oturum başında ÖNCE bunu oku** (birkaç satır, ~250 token). "Devam edelim" dendiğinde koca PLAN.md'yi okuma; buradan en son ne yapıldı + sırada ne var'ı al. Detay gerekirse PLAN.md, sonra `plans/*.md` aç.
> **Bir iş bitince:** en üste 1–3 satırlık yeni giriş ekle (yeni en üstte). En fazla ~6 giriş tut, eskiyi sil. Göreli tarih yazma, mutlak tarih yaz.

---

## 2026-08-19
- **✅ 6 ÖLÜ PERK CANLANDIRILDI — dal `feature/perk-revival` (main'den, 7 commit, MERGE/PUSH YOK), `kontrol` 1. TURDA ONAY.** Kök neden iki katmanlıydı: (1) `UpgradePanel`'in `Truck`/`PlayerMovement` SerializeField'ları sahnede **PREFAB**'a işaret ediyor (`The Main Office.unity:23018-23019`, `type: 3`), (2) tır tarafında `Truck.OnNetworkSpawn` (`Truck.cs:243-250` — eski notlardaki gibi `Awake` DEĞİL) 5 alanı SO'dan yeniden okuyup eziyor, oyuncu tarafında ise oyuncu bir kez spawn olduğu için (`PlayerSpawner.cs:293-301`) prefab yazısı canlıya hiç ulaşmıyor. **Ölü liste 6'ydı, eski notlarda 5 yazıyordu:** `prestige_broker`/`fast_hangar`/`gambler_case`/`agile_crew`/**`energetic_crew`** (atlanmıştı) + 🔴 **`all_in` TUZAK KART** — faydası (+%25 gelir) ölü, bedeli (`gracePaymentPercent=0`) çalışıyordu, yani oyuncu para verip sadece zarar alıyordu. **Çözüm:** `EventEffectManager` deseni kopyalandı — satın alma anında `FindObjectsOfType<Truck>()`/`GetOwnedPlayer()`, yeni doğan tıra `Truck.OnNetworkSpawn` kancası (SO→perk→event sırasıyla), late-join oyuncuya `PlayerMovement.OnNetworkSpawn` kancası; `UpgradePanel.Instance` singleton'ı eklendi; prefab yazısı tamamen kalktı. **economist:** hiçbir sabit değişmedi (fiyatlar zaten FAZ4 §B.7 ile sahnede güncelmiş, `f38d523f`); kritik uyarı — FAZ4 planının `prestige_broker` için istediği `+1.0/lvl` **UYGULANMADI** (4P'de değer/fiyat 8.4x'e fırlıyordu), `+0.5/lvl` korundu. `agile_crew` +%15 hız → 16 gün kümülatif gelirde +%9–14. **QA 1 BLOKLAYICI buldu, kapandı:** perk AKTİF EVENT sırasında alınırsa `EventEffectManager`'ın event-öncesi snapshot'ı event bitince perki siliyordu (`EventEffectManager.cs:558-568`) → 3 hedefli rebase hook'u eklendi (`RebaseTruckRewardPerBox`/`RebasePlayerMoveSpeed`/`RebasePlayerStaminaRegenRate`), **yalnız satın-alma yolundan** çağrılıyor — spawn/late-join yolunda çağrılsaydı çift çarpım olurdu; `ApplyEventEffectToNewObject`'i yeniden çağırma tuzağı (o `exitDelay`'i de çarpardı) bilinçli olarak reddedildi. **Doğrulama müdürde:** 0 CS + EditMode 19/19 + `EconomyInvariantCheck` 179/179 temiz (bir batchmode koşumu iki kez yarıda kesildi, bayat `Temp/UnityLockfile` silinip yeniden koşuldu — kesilmiş koşum üzerine tekrar koşmak yanıltıcı yeşil verirdi). **AÇIK (bloklayıcı değil):** `PerkAssetSnapshot`'ın Truck-prefab 4 alanı artık vestigiyel (bilinçli bırakıldı); "Stamina" backbone yükseltmesi (perk DEĞİL, draft'ta kapalı) hala prefab'a yazıyor — aynı hastalık sınıfı, ayrı iş; `gambler_case`+`high_volatility` dışlanmıyor ve çarpımsal birleşiyor (+%27 yerine +%49), playtest'te izle; `tools/economy-sim/sim.js` başlığı FAZ4 sonrası resync edilmemiş. **SIRADAKI: Unity playtest** — 6 perk canlı olduğu için `kutu/dk/oyuncu` tabanı değişti, önceki ölçüm bu perkler ölüyken alınmıştı + kullanıcı onaylarsa main'e merge.

- **🧹 EK: kalinti temizligi ayni turda kapandi.** (1) "Dinç Ekip" stamina backbone yükseltmesi aynı hastalık sınıfıydı (prefab'a yazıyordu, canlıya ulaşmıyordu) — `fix/stamina-backbone-live` dalı, mevcut `GetOwnedPlayer`/`ApplyLivePerksToPlayer`/`Rebase*` altyapısı yeniden kullanılarak bağlandı, draft'ta kapalı olduğu için economist turu gerekmedi. (2) `tools/economy-sim/sim.js` FAZ4'ten beri hiç resync edilmemişti — 11 sabit düzeltildi (kira büyüme 1.15→1.35, `prestigePerBonus` 4→8, vb.). 🔴 **ÖNEMLİ BULGU:** resync sonrası sim gösteriyor ki STRICT bantta **4P artık GÜN 16'da (son kira ödemesinde) iflas ediyor**, Slow senaryo optimistic bantta **2P/3P/4P hepsi GÜN 16'da iflas ediyor** — FAZ4'ün kira eğrisi kararı hiç doğru sim ile test edilmemiş olabilir. **Kod DEĞİŞMEDİ** (yalnız sim.js sabitleri); bu bir economist karar konusu, playtest'te 4P'nin gün 13-16 nakit akışı özellikle izlenmeli. (3) Kök `herhangi` (0 bayt) + `Assets/_Recovery/` (13 eski `.unity`) kullanıcı onayıyla silindi. **Tümü main'e merge + push edildi** (`134583d`), doğrulama tekrar koşuldu: 0 CS · 19/19 · 179/179.
- **🎤 TELSİZ AÇIK BİRAKILDI — kullanıcı kararı.** Dal `feature/voice-chat` main'e göre hem 25 commit önde hem **46 commit GERİDE**. Açık bug: client'ta WASD ölüyor, kod hâlâ geçici `[TESHIS]` log'u taşıyor (`PlayerMovement.cs`). Son commit `2655434` gerçek bir NRE bug'ını düzeltti (client'ta `_savedSettings` Awake'den önce null) ve WASD'i de çözmüş OLABİLİR ama hiç doğrulanmadı — kullanıcı testi ertelenmiş, geri dönülmemiş. **Karar: kullanıcı kendisi 2-makine testi yapacak, sonuç gelince tekrar değerlendirilecek.** Dala DOKUNULMADI.

## 2026-08-16
- **✅ ÜÇ DAL main'E MERGE EDİLDİ, AKTİF İŞ YOK — plan dosyaları bu güne kadar güncellenmemişti, şimdi senkronlandı.** `feature/post-rent-mechanics` (kira sonrası 3 özellik, kontrol ONAY), `feature/room-visibility` (oda-bazlı karartma, kontrol 2 turda ONAY), `feature/economy-verification` (yalnız tasarım dokümanı `docs/2026-07-17-economy-verification-design.md`, kod yok) — üçü de `main`'de. **N3 break-room server-auth fix'i elle taşındı (`cc04901`, `DayCycleManager.cs` +17 satır):** eski `feature/netcode-auth-hardening` dalı 63 commit geride kalmıştı, dalın 3 fix'inden N10 ve TutorialShelfState main'de zaten farklı/daha yeni şekilde çözülmüştü, yalnız N3 açıktı → o taşındı. **`feature/netcode-auth-hardening` dalı artık işlevsel olarak tükendi** — kalan 3 commit (9af0a99/01a9858/9abc294, 2026-07-16 tarihli) main'e göre stale/superseded, bekleyen gerçek iş değil; dal silinmedi, karar kullanıcıya bırakıldı. **SIRADAKİ: yok — kullanıcıdan yeni yön bekleniyor.** Açık playtest borçları hâlâ duruyor: room-visibility S7 (2 istemcili) ve post-rent-mechanics Unity playtest'i, bkz. aşağı.

## 2026-08-15
- **✅ KİRA SONRASI 3 SABİT ÖZELLİK BİTTİ — dal `feature/post-rent-mechanics` (main'den, 7 commit, MERGE/PUSH YOK), `kontrol` 1. TURDA ONAY.** Kullanıcı kararı: `plans/2026-08-13-kira-ozellikleri-fikir-havuzu.html`'deki 58 aday havuzundan seçim yapmak yerine kendi 3 sabit özelliğini belirledi — **gün5 iade** (müşteri kutu rengi gösterir, oyuncu getirir, ters yönlü akış), **gün9 2-item** (hem iade hem normal talep 2 kaleme çıkar), **gün13 iki renkli tır** (örn. 2 kırmızı+1 sarı → turuncu araç, `TruckColorMixing.cs` yeni saf sınıf). Plan: `C:\Users\cicek\.claude\plans\ilk-kiradan-sonra-m-steriler-serialized-cosmos.md`. **Gün eşiği sabitleri** yeni `PostRentFeatureUnlocks.cs`'te, `DraftPool.T2_UNLOCK_DAY`/`T3_UNLOCK_DAY` deseniyle tutarlı (RETURN=5/DUAL_ITEM=9/MIXED_TRUCK=13) — kontrol bu günlerin upgrade tier açılışıyla çakıştığını RİSK olarak not düştü (kullanıcının "her kiradan sonra" isteğinin doğal sonucu, playtest'te izlenecek, kod bulgusu değil). **Economist:** iade oranı %25, iade prestiji mevcut alanlar (customerServedPrestigeBonus/wrongProductPrestigePenalty) tekrar kullanıldı — yeni SO alanı YOK, para YOK ("para yalnız tırdan gelir" invariantı korundu); ProductSupply-dual ×1.3, BoxRequest-dual **ayrı** ×1.15 (QA'nın bulduğu çifte-uygulama hatası sonrası düzeltildi, bkz aşağı); karışık tır ödülü düz toplam, prim yok. **QA ara turu 3 gerçek bug buldu, hepsi düzeltildi:** (1) client-side mirror alanları (`_interactionMode` vb.) NGO'nun "ilk NetworkVariable senkronu OnNetworkSpawn'dan önce" tuzağına düşüp non-host client'ta bayat kalıyordu → `Truck.cs`'deki `SyncFromNetworkValues` deseni tekrar kullanıldı; (2) `RearmForSecondReturnInteraction` (2. renk beklerken) `_waitTimeStarted`'ı true'ya geri çekmiyordu → müşteri sabır sisteminden KALICI muaf kalıp istasyonu sonsuza dek bloke edebiliyordu → `StartWaitTime()` çağrısına bağlandı; (3) ekonomistin ×1.3'ü BoxRequest-dual'de (2 ayrı E-etkileşimi) iki kez bindiriliyordu (gerçek süre ~13s, varsayılan 6.5s'nin 2 katı) → ayrı `DUAL_ITEM_BOXREQUEST_INTERACTION_TIME_MULTIPLIER=1.15f` sabitiyle ayrıştırıldı. **Görsel:** iade müşterisinin üstünde renk gösteren procedural küre göstergesi (yeni sanat asseti yok, Editor GUI erişimi olmadığı için) — ProductSupply modunda tamamen gizli (Kural 1: görsel ayrım net). **Headless her turda BEN (müdür) doğruladım** (Unity 6000.5.6f1, 0 CS + 9/9 test her seferinde) — 2 subagent art arda "waiting" ile turu bitirip gerçek sonucu raporlayamadı (arka plan batchmode koşumunu doğru bekleyemedi), bu yüzden doğrulamayı devraldım; bir turda gerçek bir derleme hatası da yakaladım (Faz C'nin `Cargor.Tests.EditMode.asmdef`'e eklediği `"Assembly-CSharp"` referansı ÇALIŞMIYOR — implicit assembly asmdef referans sisteminde çözülemiyor, test dosyası silindi). **AÇIK (bloklayıcı değil, kontrol kabul etti):** `PostRentFeatureUnlocks.cs`/`TruckColorMixing.cs` için EditMode testi YOK (aynı asmdef duvarı — `Cargor.Tests.EditMode` yalnız `NewCss.Roguelite` görüyor, `Assembly-CSharp`'a referans veremiyor; devops-kapsamı bir asmdef refaktörü ister); `_plannedTruckColors` önizleme kuyruğu gün13+'ta bayat kalıyor ama okuyan kod yok; `Truck.cs` tır text UI'ı karışık talepte renk dağılımını yazıyla ayrıştırmıyor (görsel karışım/turuncu araç doğru çalışıyor, sadece metin cilası). **SIRADAKİ: gerçek Unity'de playtest** (iade göstergesinin görünürlüğü, 2-item akışının tempo hissi, ACES tonemapping altında karışık tır renklerinin doğru görünmesi) + kullanıcı onaylarsa main'e merge.

## 2026-08-13
- **🚫 KALICI KART SİSTEMİNDEN VAZGEÇİLDİ (kullanıcı kararı). Hiç kod yazılmadı, yazılmayacak.** Plan `plans/archive/2026-08-kalici-kartlar-IPTAL.md`'ye taşındı — 14 kartın onaylı değerleri ve economist gerekçeleri referans olarak duruyor ama **aktif iş DEĞİL**. Aşağıdaki 2026-08-12 girişindeki "SIRADAKİ: build komutu bekleniyor" satırı GEÇERSİZ. §4.3'teki `PerkEffect.cs` mutlak-atama bulgusu kartlardan bağımsız olarak hâlâ geçerli.
- **📅 TAKVİM EVENT SIKLIĞI — zaten sık, ek iş yok (doğrulandı).** `EventCalendarUI.cs:23-24` `EVENT_INTERVAL_MIN=1` / `EVENT_INTERVAL_MAX=3` (eski 3–5), commit'li (`d32b206`). Yani ilk 3 event'siz günden sonra ortalama ~2 günde bir event; **kira günleri (`day % 4 == 0`) bilerek atlanıyor** (`GenerateInitialEvents`), takvim 16 hücre. Sıklık tekrar değişirse ekonomiyi kaydırır → economist turu şart.

## 2026-08-12
- **🃏 KALICI KART SİSTEMİ TASARLANDI — sadece plan, KOD YOK. ⚠️ 2026-08-13'te İPTAL EDİLDİ, aşağısı tarihsel kayıttır.** `plans/archive/2026-08-kalici-kartlar-IPTAL.md`. Kullanıcı isteği: PlateUp!-tarzı, kira günü (4/8/12) sonrası ertesi gün 2 kart/oybirliği/30sn sayaç (anlaşılamazsa %50-50 rastgele)/kalıcı etki (event'lerden ayrı sistem). 20 kart önerildi → kullanıcı kira-azaltan 2'sini sildirdi, kalan havuzdan 14'ünü seçti (6 hafif-gün5/4 orta-gün9/4 ağır-gün13). **economist turu tamam:** 4 kart değer düzeltmesi (#6 ödül-%5→-%2 lopsided'dı · #11 ödül-%15→-%6, gün+%10 kapasite-sınırsız olduğu için kutu/gün'ü TAM +%10 artırıyordu, taslak net %6.5 garanti kayıptı · #16 stamina+%25→+%40, 3sn tabanda +%25 zar zor hissediliyordu · #20 talep+%20 tamamen kaldırıldı+ceza %50→%25, gün13-16'da servis kapasitesi zaten talebin altında bağlayıcı kısıt olduğu için talep artışı `served`'i hiç değiştirmiyordu), 9 kart baseline'a uygun onaylandı, #15'in kira sürşarjı sim.js'le "aşırı değil" doğrulandı (tek kalan kira gün16, compounding'le çarpışmıyor). **🔴 KRİTİK MİMARİ BULGU (§4.3):** `PerkEffect.cs` tüm perkleri idempotent MUTLAK ATAMA ile yazıyor (`+=`/`*=` yasak — `NetworkList.OnListChanged` her upgrade'de tüm client'ları yeniden tetikliyor). Kartlar aynı deseni izlerse perk+kart aynı alanı (örn. #2/#19↔agile_crew/energetic_crew moveSpeed/staminaRegenRate, #5/#9↔prestige_master, #13↔phone_line) hedeflediğinde **birikmiyor, son yazan kazanıyor, diğeri sessizce silinir** — build aşamasında gameplay'e aktarılacak, kartlar perk alanlarına doğrudan mutlak atama yapmamalı. **SIRADAKİ: kullanıcının build komutu bekleniyor** (`big-feature-plan-then-wait` — plan onayı uygulama izni değil), sonra gameplay (kart efekt katmanı + mutlak-atama çakışma çözümü) + graphics-ui (seçim modalı, ESC-menü gibi duraklama) + qa/kontrol kapısı.

## 2026-08-10
- **🕶️ ODA GÖRÜNÜRLÜĞÜ S0+S1 KODLANDI — dal `feature/room-visibility` (`main`'den), 5 commit, MERGE/PUSH YOK. 0 CS · EditMode **15/15** (8 DraftPool + **6 yeni RoomResolver** + 1 Addressables stub), 6000.5.6f1 headless, Unity kapalı.** Plan: `plans/oda-gorunurluk.md` (cherry-pick'lendi — `93dd543`+`eae3190`). **Cherry-pick çakışması planın §12'de öngördüğü gibi birebir çıktı**: `PLAN.md` + `plans/devam.md` çakıştı, ikisi `main` hâlinde bırakıldı, yalnız plan dosyası alındı.
  **S0 (`be2d3de`) — `NewCss.Rooms.Core`, motor referanssız:** `RoomBox` (saf 6-float AABB, sınır **dahil** `>=`/`<=`) · `RoomResolver.Resolve(current,x,y,z,entries,margin=0.35f)` · `RoomFade.Step` (`MoveTowards` mantığı, `Lerp` DEĞİL, tam `0f`/`1f` kilidi — H7). Üç dosyada **tek `using` bile yok** → motor referanssızlık iddiası değil, gerçek. **Histerezis sözleşmesi kodda:** 0 kutu → sentinel, exception yok · hiçbir kutuda değil → `current` **aynen döner, -1 asla üretilmez** (H8) · eşleşenler arasında `current` varsa o tercih edilir. Test asmdef'ine `NewCss.Rooms.Core` referansı eklendi (`overrideReferences:true` tuzağı).
  **S1 (`f826500`):** `RoomVolume` (OnEnable/OnDisable ile kayıt, **`FindObjectsByType` yok** → additive sahne uyumlu) + `RoomRegistry` (statik liste + `RoomEntry[]` cache, `SubsystemRegistration`'da temizlik → fast-enter-play-mode'da bayat kalmaz).
  **🔧 MÜDÜR DÜZELTMESİ (`60f1e7a`) — departman çıktısı işlevsel doğruydu ama S2'yi (senin elle oda çizme işin) pratikte zorlaştırıyordu.** 3 kusur: (K1) gizmo seçili DEĞİLKEN de dolu kutu çiziyordu → birkaç oda Scene view'ı renkli sise çevirip kutuyu duvara oturtmak için bakılması gereken geometriyi gizliyordu; artık seçilmemişken tel kafes. (K2) `worldBounds` **dünya uzayında ve transform'a bağlı değil** (bu KASITLI: §2.1'deki ölçülmüş duvar koordinatları doğrudan yazılabilsin diye) — bedeli objeyi sürüklemenin kutuyu kıpırdatmaması, **sessiz tuzak**; veri modeli korunup `Assets/NewCss/Rooms/Editor/RoomVolumeEditor.cs` eklendi (`BoxBoundsHandle`, Undo'lu, Scene view'da doğrudan sürükle/boyutlandır + Inspector'da "Y'yi cömert ver" uyarısı). (K3) yeni `RoomVolume` dünya orijininde doğuyordu (depo x[-92..-24], y≈3.88) → `Reset()` kutuyu objenin konumuna oturtuyor.
  **DOĞRULAMA BAĞIMSIZ TEKRARLANDI** (departman raporuna güvenilmedi): departman "14 test" dedi, gerçek **15** (Addressables stub'ını saymamış — yeni test sayısı yine de doğru). `Assembly-CSharp-Editor.dll`'in koşumda yeniden derlendiği teyit edildi → editör script'i sessizce atlanmadı. Batchmode artefaktları (`ProjectSettings.asset`, `UniversalRenderPipelineGlobalSettings.asset`, `ProjectAuditorSettings.asset`) her koşumdan sonra geri alındı, commit'e girmedi.
  **S3/S4/S5/S6 DE BİTTİ (aynı gün, kullanıcı "sen yaz, ben sonra tek seferde çizerim" dedi) — dal 10 commit, `kontrol` kapısı **1. TURDA ONAY**, bloklayıcı bulgu yok.** Sıralama bilerek tersine çevrildi: plan S2'yi öne koyuyordu ama gerekçesi "S3/S4/S5 *doğrulanamaz*" idi, *yazılamaz* değil → kullanıcı Unity'ye tek kez girip etkiyi anında görecek.
  **S3 `7c3be35` `RoomViewController`:** bootstrap `WorldSpaceCanvasCameraBinder` deseniyle (sahne authoring'i gerekmez), `LocalRoomId` + `LocalPlayer` yayınlar, global shader değişkenlerini besler. `RoomRegistry.TryGetBounds` eklendi (aynı `roomId`'li birden çok kutunun union AABB'si — H5). **`unscaledDeltaTime` KARARI KANITA DAYALI:** `EscapeMenuManager.cs:404` gerçekten `timeScale=0` yapıyor ama `CameraFollow.cs:264-275` kendi harita lerp'inde **ölçekli** `deltaTime` kullanıyor → karartma da ölçekli kullanıyor, yoksa duraklatmada kamera donarken karartma bağımsız ilerlerdi.
  **S4 `cf7b591` `PlayerRoomVisibility`:** merkezi manager yok, her karakter kendi odasını çözer. `PlayerInventory.Visual.cs:175-180`'e tek satır `Refresh()` (H2 — o metod 3 yerden çağrılıyor ve her seferinde YENİ renderer üretiyor; bu satır olmasa gizli oyuncunun elindeki kutu boş odada havada asılı kalırdı). **`Character.prefab` bağlandı** (GUID `.cs.meta`'dan okundu, uydurulmadı); müdür teyit etti: component kök `Character` objesinde (`9174414027654833558`), `m_Component` listesinde kayıtlı, `PlayerInventory` de aynı objede → `GetComponent` null dönmüyor, öksüz blok yok.
  **S6 `e0ed2ae`:** `NetworkStaminaBarUI` `IsOwner` gate'i — oda sisteminden bağımsız mevcut bug (aynı odadaki 3 oyuncunun barı üst üste biniyordu).
  **S5 `0058c57` — 3 shader:** globaller `CBUFFER_END`'in **DIŞINDA** (Environment 71→78 · Metal 62→71 · FlatLit 143→151) → SRP Batcher korunuyor. `FlatLit` Outline pass'i ve `ShadowCaster`/`DepthOnly` hiç dokunulmadı. `k` çarpımda en dışta → `_CargoRoomFade` tanımsız/0 iken shader **kimlik** (Tutorial güvenli). `Luminance3` FlatLit/Environment'ta zaten vardı, yalnız Metal'e eklendi.
  **🔧 MÜDÜR 2. DÜZELTME TURU (`d8522d8`):** (K4) **yerel oyuncu kalıcı kaybolabilirdi** — kendi odası İKİ ayrı histerezis makinesiyle çözülüyordu (`RoomViewController._currentRoomId` ve o karakterin `_ownRoomId`'si); controller oyuncuyu 0.5 sn arayla ARADIĞI için ikisi farklı karede/konumda ilk çözümlemesini yapabilir, çakışma bölgesinde ayrışabilir ve histerezis ayrışmayı **kilitler**. Plan §6.1 zaten "yerel oyuncu daima kendi odasında" varsayıyordu → artık kodda (`LocalPlayer` + "izleyenin kendi karakteri asla gizlenmez"). (K5) `dim`/`desat` **oyun çalışırken ayarlanamıyordu** (globaller yalnız oda/fade değişince gönderiliyordu, X basılı tutulunca fade 1'e oturup donuyordu) — oysa plan §9.3 kullanıcıdan tam olarak bunları elde ayarlamasını istiyor.
  **⚠️ TUZAK — iki Unity batchmode aynı anda çalışamaz:** shader doğrulaması ilk turda `Library\Bee\artifacts\rsp\...` **"Permission denied"** ile düştü ve log "Scripts have compiler errors" dedi — **kod hatası değil, dosya kilidi çakışması** (ajanın koşumu bitmemişken müdürünki başladı). Ayrıca **arka plan sarmalayıcısının exit kodu Unity'ninkini yansıtmıyor** (`EXIT=0` görünürken Unity `return code 1` ile çıkmıştı) → sonucu **XML'in varlığından** oku, exit kodundan DEĞİL.
  **DOĞRULAMA:** 0 CS · EditMode **15/15** · log'da `Shader error` **0** ve 3 shader'ın bu koşumda `ShaderImporter` ile yeniden import edildiği + `UnityShaderCompiler.exe`'nin çalıştığı log'dan teyitli (yani "0 hata" boş iddia değil). **`kontrol` 1. turda ONAY** — koruma bandı ihlali yok, kodda oda adı/sayısı sabiti grep'te 0, prefab YAML'ı sağlam, GUID çakışması yok.
  **🔴 YALNIZ GERÇEK UNITY'DE ÖLÇÜLEBİLİR (açık):** SRP Batcher'ın bozulmadığı (Frame Debugger önce/sonra batch sayısı) · AABB'lerin gerçek geometriyle örtüşmesi · H1'deki 5 URP/Lit materyalin karartılmayıp parlak leke bırakması · pop-in hissi.
  **S2 DE BİTTİ — kullanıcı 5 odayı çizdi** (`---ROOMS---` altında: hangar 1 · paket 2 · resepsiyon 3 · breakroom 4 · office 5). Sahne değişikliği tamamen ekleme (+282 satır, 0 silme). ⚠️ **Tır avlusu (x < -68.4) hiçbir odaya ait DEĞİL** — bug değil sözleşme: hiçbir kutuda değilken `current` korunur (H8), yani avluya çıkan oyuncu en son odasında sayılır. Avlu odası istenirse yalnız yeni bir `RoomVolume` eklemek yeter, kod değişmez.
