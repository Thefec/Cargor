# ⏭️ DEVAM — Hızlı Oturum Logu

> **Oturum başında ÖNCE bunu oku** (birkaç satır, ~250 token). "Devam edelim" dendiğinde koca PLAN.md'yi okuma; buradan en son ne yapıldı + sırada ne var'ı al. Detay gerekirse PLAN.md, sonra `plans/*.md` aç.
> **Bir iş bitince:** en üste 1–3 satırlık yeni giriş ekle (yeni en üstte). En fazla ~6 giriş tut, eskiyi sil. Göreli tarih yazma, mutlak tarih yaz.

---

## 2026-09-19 (3)
- **LOADING SCREEN 4 SAHNE GECISINE YAYILDI — kontrol ONAY (2. tur), plan `plans/loading-screen-gecisleri.md` adim 1-6 BITTI.** Yeni `LoadingScreen.cs` (DontDestroyOnLoad singleton, kendi Canvas'i sortingOrder=1000) + `Resources/UI/LoadingScreen.prefab`. Cagiranlar: `SteamManager` (delege), `GameStateManager.ExitToMenuCoroutine`, `ExitHelper`, `Menu.ConfirmTutorial` (IKI dal). NGO'nun `SceneManager.LoadScene`'ine (menu→harita) DOKUNULMADI — server-authoritative.
- **IKI BUG YAKALANDI VE KAPATILDI:** (1) departman `LoadSceneRoutine`'i statik IEnumerator yazmisti, cagiranin uzerinde donuyordu; `Menu.cs` DontDestroyOnLoad DEGIL → Tutorial yuklenince coroutine olur, `SetActive(false)` calismaz, ekran KALICI TAKILIRDI. API `void LoadScene(...)` yapildi, coroutine singleton uzerinde baslatiliyor (once `SetActive(true)` — deaktif objede StartCoroutine calismaz). (2) `Awake()` duplicate korumasi `_instance`'i hic atamiyordu, savunma kodu kirikti.
- **ONCEKI TURLAR (ayni gun):** loading screen sifirdan kuruldu (12 spinner noktasi (0,0)'da ust uste yigiliyordu — animasyon klibi projede yok; `loadingText`/`loadingProgressBar` sahnede `fileID: 0` idi). Sonra kullanici spinner'i kaldirtti → `Spinner 1` deaktif, yazi+bar ortaya alindi. `SteamManager`'da tek-render-yolu kuruldu (taban mesaj + nokta ayri state; onceden iki yazar birbirini eziyordu).
- **ADIM 8 ACIK:** `MainMenu.unity > Canvas > LoadScreen` artik olu (hicbir kod tetiklemiyor), kaldirilmasi AYRI TUR.
- **KAPSAM DISI BULGU (kullaniciya raporlandi):** `Assets/Scenes/Tutorial.unity`'de oturum-oncesi bir degisiklik var — bir `RedBoxSlot`'un `redBoxItemData`'si `RedBox.asset` → `RedBoxOpen.asset` olmus. Kasitli mi bilinmiyor, tutorial'da yanlis kutu spawn edebilir. COMMIT EDILMEDI.
- **COMMIT DISI BIRAKILANLAR (degismedi):** `Tutorial.unity`, `Packages/manifest.json`+lock (unity-mcp + ai.assistant gelistirici paketleri, netcode 2.13.0→2.13.2), `ProjectSettings.asset` (kisisel cloud baglamasi), `link.xml` silinmesi. Netcode yukseltmesi ile `Tutorial.unity` ATOMIK bagli — ikisi birlikte, test edilerek gitmeli.

## 2026-09-19 (2)
- **LOBI ID EKRANDA GORUNMUYORDU — DUZELTILDI, kontrol ONAY (1. tur), tek dosya `Assets/NewCss/Steam/SteamManager.cs` (+58/-1), COMMIT YOK.** Kok neden: 2026-08-22 lokalizasyon turunda `LocalizationAutoWire.cs` her TMP'ye `LocalizeStringEvent` takmis; `MainMenu.unity`'de `'Lobby ID: 83942'` objesinin persistent `set_text` call'u (`m_CallState:2` = RuntimeOnly, bu yuzden Editor'da gorunmuyor) `InLobbyMenu.SetActive(true)` aninda `UpdateLobbySaver`'in yazdigi kodu eziyordu; tablo degeri sadece etiket ("Lobby ID:" / "Lobi ID:", anahtar `LobbyId`, format argumani yok).
- **COZUM:** etiket `OnUpdateString` runtime listener'i ile ayri yakalaniyor (`_lobbyIdLabel`), kod ayri tutuluyor (`_lobbyCodeDisplay`), `ApplyLobbyIdText()` birlestirip yaziyor. UnityEvent'te persistent call'lar runtime listener'lardan once calistigi icin son yazan hep biz oluyoruz → OnEnable / dil degisimi / tablo gec yuklenmesi senaryolarinin ucu de kapali. Sahne ve ceviri tablolari DEGISMEDI. Unity `validate_script`: 0 hata/0 uyari.
- **AYNI SINIFTAN ACIK KALAN (kullanici kapsami daraltti, YAPILMADI):** 4 oyuncu slot ismi TMP'si de `LocalizeStringEvent` tasiyor ve literal "Player" string'ine bagli (comp fileID 827032992 / 1827421890 / 856221037 / 152244532) → dil degisiminde Steam isimleri "Player"a doner. Ayrica `LocalizationAutoWire.cs` dislama listesi yok, bug geri gelebilir. Ayrica sahnede `ErrorMessageText`/`loadingText`/`loadingProgressBar` = fileID 0 → tum hata mesajlari ve yukleme yazilari sessizce yutuluyor.

## 2026-09-18 (2)
- **(4. oturum) OPTİMİZASYON TEFTİŞİ — plan `plans/optimizasyon-teftisi.md`, kullanıcı onayıyla 4 madde uygulandı (Log stack trace kapalı · StaminaBar cache · CustomerAI anim NetworkVariable eşiği · varsayılan kalite ULTRA→HIGH), derleme temiz, alt ajan yok, COMMIT YOK.** Outline bulgusu yanlış çıktı (zaten statik cache), iptal.
- **(3. oturum) TELEFON PARASI KALDIRILDI (#2) + SESSİZ ANLAR (#3) — ayni dalda, KONTROL KAPISINDAN GEÇMEDİ (kullanıcı bütçe kararı: alt ajan yok), derleme temiz.** Economist: `callMoneyReward` 20→0 (cs + `EkonomiAyarlari.asset` + sim + GDD §14.4 + `EconomyInvariantCheck.cs:337`), prestij +0.4 ve müşteri çağırma kaldı; Slow/strict %100 spam optimumu gitti. TR+EN tutorial metinleri (telefon sayfası eski "çalan telefon" modelini anlatıyordu) düzeltildi, diğer 15 dil BAYAT. graphics-ui: `para_A`/`dogru_A` → SfxLibrary id 8/9 (B/C import edildi, atanmadı), yeni `SfxId.CustomerLost` id 10 + `CustomerAI.HandleTimeUp` ClientRpc — klip WrongItem PLACEHOLDER (ayırt edici klip yok, ffmpeg de yok).
- **ZORLUK ÖLÇEKLEME (denetim #1) + KES LİSTESİ (#4) BİTTİ — kontrol ONAY (2. tur), dal `fix/difficulty-scaling-and-dead-code`, COMMIT EDİLMEDİ (kullanıcı kararı bekleniyor).** İki kusur: (a) `DifficultyManager` artık connect/disconnect callback'lerine abone, sayı `ConnectedClientsList.Count` ile yenileniyor (NGO kaynağında remove→invoke sırası doğrulandı); (b) sabır spawn anında `CustomerManager.SpawnCustomer`'da, stamina regen **owner-side** `PlayerMovement.OnNetworkSpawn`'da uygulanıyor (düz alan → server-side atama uzak client'a ulaşmıyordu, kontrol 1. turda yakaladı). Economist: gerçek canlı değer prefab'daydı (8/14, cs default'u 35/55 hiç canlıya çıkmamış), 15/20/−2 yapıldı → 1P 15-20s = eski solo his, 4P 9-14s. Silinen ölü kod: `MENUUI/Lobby/{LobbyManager,MainMenuUI,NetworkPlayerData,PlayerSlot}.cs` + `Network/NetworkObjectPool.cs` (0 sahne/prefab ref, "GameScene" sahnesi yok). GDD §19.1 güncellendi.
- **AÇIK:** `ScaledCustomerCount` hâlâ ölü (kota tablosu belirliyor); `TutorialCustomerManager` sabrı kendi set ediyor (aynı kusur olabilir); `PlayerSpawner.cs:309-313` server-side atama artık vestigial. Gerçek 2-client late-join playtest yapılmadı.
- **SIRADAKİ:** kullanıcı Unity'de host+geç katılan client ile test edip commit/merge kararı verecek. Denetim #2 (telefon +20TL) ve #3 (sessiz ödül/ceza) hâlâ açık.


## 2026-09-18
- **TASARIM DENETIMI BITTI — kontrol ONAY (1. tur), KOD DEGISMEDI, COMMIT EDILMEDI.** Kullanicinin yarin dolan ~$29 claude.ai usage credit'i, 6 paralel salt-okunur ajanla tasarim denetimine harcandi (A1 determinizm / A2 tekrar egrisi / A3 mekanik envanteri / A4 geri bildirim / A5 tur kiyasi / B1 netcode). Turkce sentez: `docs/playtest/tasarim-denetimi-2026-09-18.md`, alt raporlar `docs/playtest/denetim-2026-09-18/`, plan `plans/oyun-tasarim-denetimi.md`.
- **TESHIS:** oyun statik degil cunku tekrar ediyor — cunku **sonuc onemsiz**. Kosu kaybedilemez (A1: 40.000 denemede 0 iflas), zorluk oyuncu sayisina hic olceklenmiyor (IKI ayri kusur: `DifficultyManager.cs:172-184` bayat snapshot + `:425-441` prefab ezilmesi, ikisi de duzelmeden calismaz), telefon kosulsuz +20TL basiyor, 16 gunun 11'i yapisal olarak ayni, 25 yukseltmenin 6'si erisilemez, en buyuk odul+ceza sessiz.
- **SIRADAKI:** kullanici oncelik secip is acacak. Ilk uc madde bug (zorluk olcekleme / telefon odulu / sessiz odul-ceza), kalan uc madde tasarim karari (11 duz gun / 6 devre disi yukseltme / kaybedilemezligin koku). Ekonomik degerli maddeler once economist'e.

## 2026-09-16 (2)
- **RAF KUTU PICKUP + BANT ELDE-BÜYÜME REGRESYONU DÜZELTİLDİ, kontrol ONAY, COMMIT EDİLMEDİ (kullanıcı playtest bekleniyor).** `626a1df`'in yan etkisiydi: collider'ı "Visual" alt objeye taşıyan prefab'larda (RedOpen/BlueOpen/YellowOpen/DuctTape, NGO+Normal) kod hâlâ `collider.GetComponent<NetworkWorldItem>()` ile root'ta arıyordu → hep null → kutu menzile hiç girmiyordu. Fix: `PlayerInventory.Detection.cs:56`, `Shelf.cs:501`, `ItemDispenser.cs:349` → `GetComponentInParent`. Ayrıca `Normal/DuctTape.prefab` scale'i 626a1df'te unutulmuş (98.69 kalmış, NGO kardeşi 8.5'e kalibre edilmişti) → 8.5'e çekildi.
- **DERS:** "Visual alt objeye taşı" deseni uygulanırken collider'a bağımlı TÜM `GetComponent` çağrıları (birincil VE fallback path'ler) taranmalı — qa ilk turda ana bug'ı geçirdi ama ikincil fallback'i (Shelf/ItemDispenser slot-doluluk kontrolü) ayrı bulguyla yakaladı.
- **SIRADAKİ:** kullanıcı Unity'de host başlatıp raf/bant test edecek, onay verirse commit edilecek.

## 2026-09-16
- **PAKETLEME ODASI BOŞ KUTU + BANT DİSPENSER BUG'I ÇÖZÜLDÜ, COMMIT `626a1df` main'e (PUSH YOK).** Kullanıcı raporu: raflarda boş kutu yok, bant masasında bant yok. Kök neden ikiliydi: (1) 4 yeni FBX (B/R/Y_Open_Box, Duct_Tape) `useFileScale:0` ile import edilmiş → mesh 17x-113x büyütülmüş, üstüne eski kutu prefab'ının ~98.69x compensating scale'i binince devasa çıkıyordu — `useFileScale:1`+`globalScale:1` yapılıp projenin diğer tüm FBX'leriyle aynı konvansiyona getirildi (DuctTape scale ayrıca 98.69→8.5). (2) Kutuların rafta/elde/masada yanlış duruşu: `Instantiate(prefab,pos,rot)` VE `PlayerInventory.Visual.cs`'nin elde-tutma kodu root Transform'un rotasyonunu HER YERDE eziyor — prefab köküne rotasyon vermek işe yaramıyordu. Çözüm: mesh+collider ayrı bir "Visual" alt objeye taşındı, o alt objeye -90 X sabit tilt verildi (6 prefab: RedOpenNGO/BlueOpenNGO/YellowOpenNGO + RedOpen/BlueOpen/YellowOpen). Kod tarafı hiç değişmedi, her yerleşim noktasında otomatik tutarlı.
- **DERS (kaydedildi):** [[unity-fbx-import-scale-mismatch]] ve [[unity-item-rotation-override-pattern]] — ikisi de tekrar edebilecek genel Unity tuzakları, yeni item eklerken kontrol listesi olarak kullan.
- **SIRADAKI:** kullanıcı Unity'de tekrar test edip onay verdi, aktif açık iş yok.

## 2026-09-15 (2)
- **VOICE-CHAT WASD BUG'I KAPATILDI (kullanici karari, `1f3210b`) — KANIT ZAYIF.** Kullanici 2 makine/hesapla host+client oynadi, bug'i gormedi. `AppData/Local/Unity/Editor/Editor.log`'daki 264 `[TESHIS]` girisini inceledim: net kanit yok (3 supheli-tek-kareli kayit, kalici donma degil; LockMovement sayaci 12/10 ama log gurultulu — gun boyu coklu Play oturumu + benim menu hata ayiklamam karismis). Bug intermittent oldugu icin tek test kesin kanit degil diye acikca soyledim, kullanici yine de kapatmaya karar verdi — PlayerMovement.cs'deki tum teshis kodu silindi. **Tekrar ederse sifirdan teshis kurulmali**, referans: `328e7e7`.
- **SIRADAKI:** aktif olarak acik bir is kalmadi. Hafiza + devam.md guncel.

## 2026-09-15
- **`feature/audio-system` MAIN'E MERGE + PUSH (`8980f35`).** Onceki oturumdan devam: kullanici "ses dalini merge et" dedi. Merge oncesi 37 dosyalik, isle ilgisiz onceden-var-olan kirli calisma alani (GDD.md, Packages, ProjectSettings, RP asset'leri, 6 script) `git stash -u` ile kenara alindi, main'e gecilip cakismasiz merge yapildi, push edildi, feature dali silindi, stash geri poplandi — hicbir sey kaybolmadi.
- **HAFIZA TEMIZLIGI — 4 dosya BAYATTI, main'e coktan merge edilmis dallari "merge bekliyor" diye tutuyordu:** `feature/room-visibility` (`d852ebd`, 2026-08-16) · `feature/post-rent-mechanics` (`9f0e575`, 2026-08-16) · `feature/perk-revival` (`d122e4c`, 2026-08-19) · `feature/voice-chat` (tamami main'in icinde, `git merge-base --is-ancestor` ile dogrulandi). Hepsi `git merge-base`/log ile TEYIT EDILEREK duzeltildi (MEMORY.md + ilgili 4 dosya). 3 bayat yerel dal (`number-roll-animation`, `tutorial-rewrite`, `netcode-auth-hardening`) da zaten merge oldugu icin silindi.
- **TEK GERCEKTEN ACIK IS: voice-chat WASD bug'i, main'de hala cozulmemis.** `PlayerMovement.cs:676,805` teshis kodu (`[TESHIS]`, `MovementDiagnostics` bayrakli) main'de canli bekliyor. Sirasi: DEVELOPMENT_BUILD ile 2-client reprodüksiyon + `Player.log`'daki `[TESHIS]` satirlari lazim (`plans/playtest-checklist.md` B1).
- **DERS:** merge'lenen dallar sonrasi hafiza dosyalari GUNCELLENMIYOR — bir dal merge edildiginde o turda ayni oturumda ilgili memory dosyasi da duzeltilmeli, yoksa haftalar sonra "merge bekliyor" yanlisligi birikir.
