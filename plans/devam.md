# ⏭️ DEVAM — Hızlı Oturum Logu

> **Oturum başında ÖNCE bunu oku** (birkaç satır, ~250 token). "Devam edelim" dendiğinde koca PLAN.md'yi okuma; buradan en son ne yapıldı + sırada ne var'ı al. Detay gerekirse PLAN.md, sonra `plans/*.md` aç.
> **Bir iş bitince:** en üste 1–3 satırlık yeni giriş ekle (yeni en üstte). En fazla ~6 giriş tut, eskiyi sil. Göreli tarih yazma, mutlak tarih yaz.

---

## 2026-07-15
- **SHADER GÖZDEN GEÇİRME (gün-sonu, müdür inline, max effort, token-bilinçli) → 1 ÖLÜ-KOD FİX COMMIT'Lİ (`d518424`, dal main'den 3 önde).** Kullanıcı 4 FlatLit shader'ında mantık hatası/alakasız kod istedi (görüntü bozmadan). **Tek gerçek bulgu düzeltildi: `FlatLitProps` `_DetailContrast` slider'ı etkisizdi** — `detailMask` hesaplanıp hiç okunmuyordu (ölü); property+CBUFFER+ölü satır kaldırıldı, render çıktısı DEĞİŞMEDİ (tek pass, self-consistent CBUFFER). Materyal orphan `_DetailContrast:1.5` (2 .mat) Unity'ce yok sayılır, dokunulmadı. **İNCELENİP KASITLI/GEREKLİ diye BIRAKILANLAR (silmek görüntüyü bozar/derlemeyi kırar):** `InputData inputData` = Forward+ `LIGHT_LOOP_BEGIN` makrosu kullanıyor (silinemez); ana ışık `mainLight.color`/şiddet çarpmıyor = sevilen flat-toon stilin ana sebebi (kasıtlı); additional `*0.5`, `saturate` yerleşimi, outline `normalize(normalCS.xy)` = kasıtlı/kabul; `_BaseMap_TexelSize`/`_NormalMap_ST`/`lumC` = Unity-auto/derleyici-strip önemsiz kalıntı. Editor açıktı → headless yok, ama salt ölü-kod çıkarımı derlemeyi kıramaz (0 kalan ref grep'le teyit). **SIRADAKİ:** kullanıcı Unity Console teyit + shader play-test · dal main'den 3 önde (yumuşak cel + props temizliği + doküman): beğenirse merge/push · sonra ekonomi turu.
- **FLATLIT SHADER YUMUŞAK CEL — COMMIT'Lİ (`f3bb679`, graphics-ui + kontrol ONAY 1 tur, dal `feature/map-mechanics-balance` main'den 1 önde). MERGE/PUSH YOK, kullanıcı gözle play-test bekliyor.** Kullanıcı bildirdi: gün-gece ışığı gezerken harita/ağaç renkleri kare-kare/bölge-bölge sert geçiyor. **Tanı: shader, ışık DEĞİL** — `AutoLightController` şiddetleri Lerp'liyor (yumuşak), sertlik 4 FlatLit shader'ının `floor(NdotL*3)/3` cel kuantizasyonundan (low-poly düz normal → yüzey topluca banttan banta zıplar; ana ışık terimi renk/şiddet çarpmıyor, sadece yön/NdotL → akşam kararması güneş **dönüşünden**). **Fix (kullanıcı 'yumuşak cel' seçti):** sert `floor` → `smoothstep` yumuşak cel, yeni `_CelSmoothness` (Range 0-1, default 0.5; 0=eski sert). 4 shader (FlatLit/Environment/Metal/Props), FlatLit 2-pass CBUFFER senkron (SRP Batcher), sıfıra-bölme guard `max(_,1e-4)`, additional-lights `addCel` de aynı. Ekonomik değer yok (salt görsel). Editor açıktı → headless yapılamadı, kontrol statik ONAY. **SIRADAKİ:** kullanıcı Unity'de bak + `_CelSmoothness` material'da zevkine ayarla · beğenirse merge/push · sonra ekonomi turu.
- **🔖 TELEFON İŞİ KAPANDI + DAL MAIN'E MERGE (local FF).** Play-test: telefon çalıyor/açınca ödül/açmayınca cezasız ✅ + wait bar fix ✅. **Wait bar bug'ı düzeltildi COMMIT'Lİ (`c105b31`):** PhoneWaitBar aptal görünümdü (SetFillAmount tek sefer, kendi Update yok) → dolgu donuk. Yerel görsel geri-sayım eklendi (StartCountdown+Update 1→0, salt görsel, server otorite). Editor açıktı → headless atlandı, Unity canlı derledi, play-test ✅. **MERGE + PUSH TAMAM: `git branch -f main HEAD` (FF, çalışma ağacı/Editor'e dokunmadan; dal main'den lineer 11 commit öndeydi) → `git push origin main` (`795ea45..659f317`).** Dal silinmedi. **KULLANICI KARARLARI:** (2-proximity) BOŞ VER/kabul — arkadaş co-op, hile anlamsız, kod değişikliği yok. (2-oyuncu ölçeği) FLAT KALSIN (müdür kararı) — economist flat EV dengeledi, ölçek=doğrulanmamış yeni değer; gelecek ekonomi turuna not. (telefon ödül ince ayarı) ekonomi turuyla sonra. **SIRADAKİ:** push kararı · sonra ekonomi turu (telefon ödül + oyuncu-ölçeği dahil topluca) VEYA yeni iş kolu.
- **TELEFON SİSTEMİ PASİF/REAKTİF YENİDEN TASARIM — 3 COMMIT (`2b32787` feat + `97d5294` Table fix + `2165f2b` docs, dal `feature/map-mechanics-balance`, economist+qa+kontrol ONAY 1 tur, headless 0 CS 2×). MERGE/PUSH YOK.** Proaktif (oyuncu E→arama+para+zaman-atlama) → server-inisiyatifli pasif: saatte %30 çalar, açarsa para(20)+prestij(0.5), açmazsa CEZASIZ. GameEconomySettings eski 4 alan silindi→yeni 5 alan; PhoneCallManager tam yeniden yazım (server-auth `_isRinging` NV, double-reward guard). economist perk-eşlemesini additive'e çevirtti: yeni `phoneRingPerkBonus=0.15` (taban mutasyonsuz), `GetEffectiveRingChance` üst-clamp 0.65 → perk+event 0.60 (çarpımsal 0.675 değil). CUSTOMER SUPPORT event Negative→Positive (cezasız olunca bonus). FESTIVAL DAY host çift-OnNewDay idempotency guard. **⚠️ devam.md aea0266'da stale'di — arada loglanmamış 4 commit vardı:** `0c026ef` kota-kaldır, `f6967ca` prestij clamp150 (`prestige_cap_bug_and_fix.md`), `bf302c5` SURPRISE AUDIT+FESTIVAL, `3b3c3ae` masa outline. **KULLANICI KARARINA BIRAKILAN 2 BACKLOG (qa non-blocker, commit'i bloklamadı):** (1) proximity server-doğrulaması yok — `AnswerServerRpc` çağıranın telefon yanında olduğunu doğrulamıyor (ön-var-olan desen, sınırlı: 1 ödül/çalma; sertleştirme server-collider→play-test bağlı). (2) difficulty-scaling kaybı — `SetCallChance` no-op, oyuncu-sayısı ölçeği kalktı (flat-EV ile tutarlı ama dokümante değil). **KALAN:** play-test (telefon çalıyor/açınca ödül/açmayınca cezasız + perk+event birleşimi) · merge+push kararı (dal main'den 9 commit önde) · yukarıdaki 2 backlog kullanıcı kararı.

## 2026-07-14
- **ANA HARİTA CEZA MEKANİK DENGELEME — COMMIT'Lİ (`aea0266`, dal `feature/map-mechanics-balance`, economist + kontrol ONAY 1 tur, headless 0 CS). MERGE/PUSH YOK — kullanıcı kararı bekliyor.** Kullanıcı 4 mekaniği (ceza/kırma/raf/tır) analiz istedi → mikro-tutarsızlıklar bulundu (makro denge zaten sağlıklı, economy-audit). Düzeltmeler: (1) **kutu/ürün düşme para cezası merkezileştirildi** — `GameEconomySettings.boxDropMoneyPenalty=5` (economist); önceden her prefaba gömülü+tutarsızdı (BlueNGO=1, RedNGO/YellowNGO=5, ürünler=5, eski=10). `BoxFallPenalty.dropMoneyPenalty` SO-backed property'ye çevrildi (prestij deseni). (2) **Kırma/düşme cezası yüzey-bağımsız+eşik-hizalı** — IsGroundCollision kaldırıldı (yer/duvar/raf aynı), eşik BoxDestroy=3 ile senkron (yumuşak bırakma <3 cezasız). Ölü hasBeenThrown/SetThrown sadeleşti, mojibake yorum temizlendi. (3) **Yanlış renk teslim prestij cezası** `wrongDeliveryPrestigePenalty=-0.2` (Truck.ProcessWrongDelivery, server-only). (4) `penaltyPerBox` default 60→40. **Dolu kutu DEĞİŞMEDİ** (kırılmaz+cezasız, kullanıcı kararı). Plan: `plans/iterative-stargazing-yeti.md` (aslında `~/.claude/plans/`). **KALAN:** prefab orphan `dropMoneyPenalty` YAML anahtarları (Unity reserialize temizler) · play-test (boş kutu duvar vs yer aynı ceza / yanlış teslim prestij düşer / dolu kutu kırılmaz regresyon) · merge+push kararı (dal main'den 1 commit önde).
- **🔖 FAZ 0 KAPANDI — roguelite → main MERGE + PUSH tamam (`5833070`).** Kullanıcı onayıyla (play-test ✅ + "merge & push"): (1) 4 gameplay-fix commit `629d436` (dolu kutu kırılma / tır rengi URP / müşteri ürün yerleşimi). (2) **11 NGO prefab serileştirme senkronu commit `6bbeeb6` — devam.md'nin "gürültü, revert" teşhisi YANLIŞTI:** `onlyIfEmpty` kaldırma + BoxFallPenalty `economySettings` (EkonomiAyarlari.asset) ref meşru script-senkronu; revert edilseydi Unity yeniden dirty yapardı (runtime `Resources.Load` fallback zaten var, kırılma değil hijyen). (3) editör-only yerel co-op test aracı commit `76439f5` (build'e girmez). (4) **gerçek artefaktlar revert:** 8 font + LiberationSans + ProjectSettings CRLF + **sahnedeki test-NetworkManager/Facepunch Spacewar app 480** (test kurulumuydu, production'a girmemeliydi). (5) `--no-ff` merge `5833070` (tek-nokta revert edilebilir) → `git push origin main` ✅ (`0d2c37c..5833070`, +73 commit). Headless derleme 0 CS. **Kalan untracked:** `_Recovery/0 (10).unity` junk (dokunulmadı). **roguelite dalı silinmedi** (kullanıcı kararına bırakıldı). **SIRADAKİ:** FAZ 1 kalan güvenli işler (N3 break-room client-auth büyük; latent P2 G23/G28/N8/N9/N10; TutorialShelfState G-item eşleniği) VEYA FAZ 2 ekonomi/FAZ 3 Steam çıkış — kullanıcı önceliği.
- **🔖 OTURUM KAPANDI (save). 5 TEMİZ COMMIT + DOKÜMAN DENETİMİ + LATE-JOIN FIX + TEST GEÇTİ.** Commit'ler: (1) `8002950` **fix(kutu+ui):** dolu-kutu-parçalanma (NetworkWorldItem isFull skip + PlayerInventory.Visual ApplyServerAuthoritativeBoxInfo) + NaN-frustum (WorldSpaceCanvasCameraBinder). (2) `49727f6` **docs:** tüm .md denetimi. (3) `b20a48a` **chore(prefab):** 6 dolu kutudan BoxFallPenalty (kullanıcı kasıtlı) + RedFull isFull. (4) `1e03eff` docs(faz0). (5) `54c5ed2` **fix(netcode): late-join reddi UX** — SteamManager fix'i **[NETDBG] temizlenmiş** commit'lendi (35 satır teşhis çıkarıldı, fix mantığı değişmedi, 0 dangling ref); LateJoinGuard tamamen HEAD'e revert (hepsi [NETDBG]'ydi). Kutu/NaN/late-join play-test ✅. Ekonomi: kullanıcı "sistemler bitince sıfırdan birlikte" → ertelendi.
- **İKİ BUG FIX (müdür inline — kullanıcı subagent'ı reddetti, "devam et").** (1) **Dolu kutu fırlatınca parçalanıyor** — İLK fix yanlış script'i hedefledi (isFull/BoxDestroy), fırlatınca hâlâ kırıldı → Phase-1 tekrar. **GERÇEK kök neden KESİN:** İki kırılma script'i var; DOLU kutu dünya prefabları (NGO/RedNGOFull vb.) `BoxDestroyOnCollisionNetcode` TAŞIMIYOR (yalnız boş NGO/RedNGO taşıyor), sadece `NetworkWorldItem` var → `_hasBoxDestroyScript=false` → `NetworkWorldItem.OnCollisionEnter` çalışıp fırlatılan kutuyu **isFull'e bakmadan** ürün gibi kırıyordu. **Fix: `NetworkWorldItem.OnCollisionEnter`'a `if(boxInfo!=null && boxInfo.isFull) return` eklendi** (IsServer guard'ından sonra, _wasThrown'dan önce). Boş kutu etkilenmez (zaten _hasBoxDestroyScript ile atlanır). Yardımcı önceki fix'ler duruyor: `ApplyServerAuthoritativeBoxInfo` (worldPrefab BoxInfo'dan isFull okur) + `RedFull.prefab isFull→1` → isFull server'da güvenilir true, yeni kontrol çalışır. (2) **Client'ta durmadan `-nan(ind) frustum` spam'i** — kullanıcı düzeltti: rafa koymayla İLGİSİZ, client join anından itibaren sürekli. Kök neden KESİN: "The Main Office"de 11 world-space canvas'ın hepsi Event Camera'sız (`m_Camera:0`); 2'sinde GraphicRaycaster var (**"Poster"** + **"Canvas"**). Netcode'da oyuncu kamerası runtime-spawn → client'ta `Camera.main` join anında yok → raycaster projeksiyonu NaN → spam. Fix: `Assets/NewCss/UIScripts/WorldSpaceCanvasCameraBinder.cs` (self-bootstrap `[RuntimeInitializeOnLoadMethod]`, sahne edit'i yok, worldCamera'sı null world-space canvas'lara `Camera.main` bağlar). **SIRADAKI: kullanıcı client play-test (dolu kutu fırlat→kırılmıyor mu + NaN spam kesildi mi); Unity kapalıysa headless 0 CS teyidi; sonra kontrol kapısı + seçici commit.**
- **YEREL CO-OP TEST ARACI eklendi (COMMIT'SİZ, editör-only, derleme 0 CS).** Steam'siz tek makinede client testi için: `Assets/NewCss/Network/LocalCoopTestBootstrap.cs` — tamamı `#if UNITY_EDITOR` (build'e girmez). Menü toggle `Tools ▸ Cargor ▸ Local Coop Test (127.0.0.1)`; açıkken Play'de OnGUI HOST/CLIENT butonları → UnityTransport 127.0.0.1'e geçer, `ConnectionApproval=false` + callback=null ile Steam whitelist/geç-join reddini baypas eder, StartHost/StartClient. MPPM 2.0.2 zaten kurulu (ParrelSync gerekmez). **Kilit gerçek:** `PlayerSpawner` Steam'e bağlı DEĞİL (NGO ConnectedClients+clientId ile spawn) → Steam'siz karakter spawn olur. NetworkManager "The Main Office" sahnesinde. **Kullanıcı test edecek:** toggle aç → The Main Office Play → MPPM sanal oyuncu → ana=HOST, sanal=CLIENT. Steam'siz boot'ta NRE çıkarsa iterasyon gerekebilir (SteamManager.Instance null olabilir). Headless derleme 2x doğrulandı (0 CS).
- **OYNANIŞ BUG FIX #3 (COMMIT'SİZ, gameplay dept + kontrol ONAY 1 tur) — client'ta müşteri ürün yerleşimi kayması.** Client tarafında müşteri masaya ürün bırakınca scale-up yerine origin'den masaya kayıyordu. Kök neden: `CustomerAI.PlaceProductOnDropOffTable` ürünü pozisyonsuz Instantiate→Spawn origin'de → NetworkTransform `Interpolate:1` server-auth client'ta origin→slot interpolasyonu; ayrıca scale-up `PlayOn` server-only (ClientRpc yok, DisplayTable düz MonoBehaviour). Fix (Table.cs/ShelfState.cs desenini taklit): spawn ÖNCESİ `dropOffTable.SlotPoints[slotIndex]` pozisyonunda Instantiate + yeni `[ClientRpc] PlayPlacementAnimationClientRpc` (race-guard'lı) → tüm client'larda tek scale-up. `DisplayTable.PlaceItemInstance`'a `playAnimation=true` default param (geriye uyumlu) + `SlotPoints` public getter. Edge-case (müşteri 0.3s içinde despawn → coroutine host `this` ölür) kontrol'de incelendi: müşteri ürün alınana kadar süresiz bekliyor → blocker değil (gün-sonu zorla-çıkış teorik düşük risk, önceden mevcut). Headless derleme UNITY AÇIK olduğu için yapılamadı — kullanıcı Unity kapatınca 0 CS teyidi + client play-test borcu.
- **OYNANIŞ BUG FIX (COMMIT'SİZ, çalışma ağacında, kontrol ONAY 1 tur) — kullanıcı bildirdi, play-test bekliyor.** İki bug: (1) **Tır renk uyuşmazlığı** — `Truck.SetObjectColor` `renderer.material.color` kullanıyordu, URP shader'da `.color` görünür renge map olmaz → tır sanat-varsayılanı (kırmızı) kalıyor, text doğru tipi (mavi) gösteriyordu. Fix: `GetComponentInChildren<Renderer>` fallback + `_BaseColor`/`_Color`/`.color` üçlü yazım. Renk MANTIĞI zaten doğruydu (enum Yellow=0/Blue=1/Red=2, prefab refs atanmış). **CAVEAT: tır hâlâ kırmızı kalırsa materyal kırmızı-albedo TEXTURE'a sahip demektir → art işi (beyaz base gerek), kod değil.** (2) **Dolu kutu fırlatınca saymıyor+parçalanıyor** — `BoxDestroyOnCollisionNetcode` `onlyIfEmpty=false` → dolu kutu tıra çarpınca trigger'a girmeden kırılıyordu. Fix: dolu kutu ASLA kırılmaz (`if(isFull) return`), `_wasThrown` kapısı kaldırıldı → boş kutu bırakınca da fırlatınca da kırılır (hız eşiği 3 korundu). `MarkAsThrown` no-op'a çevrildi (imza korundu). BoxFallPenalty'ye DOKUNULMADI (ekonomi değişmedi). **SIRADAKI: kullanıcı play-test (tır rengi mavi tutuyor mu + dolu kutu tıra girip sayılıyor mu + boş kutu bırakınca kırılıyor mu), sonra seçici commit.**
- **G6/G7 envanter/masa/raf kimlik-doğrulama exploit'leri KAPANDI COMMIT'Lİ (`0254202`, qa denetim + kontrol ONAY 1 tur, kullanıcı "tam fix" onayı).** Sistemik `RequireOwnership=false` teması. qa denetimi kapsamı büyüttü. **İki açık:** (1) item fabrikasyonu — `SetInventoryState`/`GiveItemDirectly`/`ShelfState.TakeItemFromShelf` public+ownerless ama yalnız server-side giriş-RPC'lerinden çağrılıyordu → doğrudan-ağ exploit → G2 deseniyle server-only düz metoda çevrildi (`if(!IsServer) return`, çağıranlar güncellendi, `requesterClientId` giriş-RPC'de gerçek sender'dan türetiliyor). (2) başka-oyuncu-envanteri — `Table.RequestInteractionServerRpc` `playerNetworkId`'yi owner ile doğrulamıyordu → `ValidateInteractionRequest`'e `sender==hedef-owner` çapraz-kontrol. **Ölü RPC silindi:** `PlaceItemOnShelfServerRpc`(G6)+`ClearCurrentItemServerRpc`(G7), 0 çağıran. 5 dosya seçici commit (hassas dosyalar korundu), headless 0 CS, ekonomik değer değişmedi. **PLAY-TEST BORCU (kullanıcıda): masa/raf al-koy-kutula co-op'ta çalışıyor mu.** **YENİ BACKLOG (kontrol notu): `TutorialShelfState.TakeItemFromShelfServerRpc` aynı açığın tutorial eşleniği — düşük risk, ayrı G-item.** **SIRADAKI açık P0 (büyük, play-test'siz güvenli değil — server-presence refactor): N3 break-room client-auth. Kalan P2 latent: G23/G28/N8/N9/N10.**
- **G1-b ham-delta para exploit'i KAPANDI COMMIT'Lİ (`db133e7`, qa 7-çağıran denetim + kontrol ONAY 1 tur).** G1 teması artık TAM (G1-a+G1-b). `MoneySystem.ModifyMoney` client-branch'i ham delta'yı `ModifyMoneyServerRpc [ServerRpc(RequireOwnership=false)]` ile yolluyordu → `ModifyMoney(999999)` exploit. **G16 sonrası engel kalktı:** qa denetimi 7 meşru çağıranın (UpgradePanel Reroll/Purchase, DayCycle Update-IsServer, QuestManager server-guard, Truck.ProcessDelivery, PhoneCall RequestCall, BoxFallPenalty) HEPSİ server-context → relay ölü kod. **Fix:** `ModifyMoney` server-only (G1-a SetMoney/ResetMoney deseniyle simetrik `if(!IsServer) LogWarning+return`), `ModifyMoneyServerRpc` silindi, clamp+para değerleri değişmedi, AddMoney/SpendMoney otomatik server-only. **SIRADAKI açık P0/P1 (play-test'siz güvenli): N3 break-room client-auth (büyük), G6 ShelfState place-item ownership, G7 envanter-RPC sender==owner.**
- **G2 tır teslim-kanıtı exploit'i KAPANDI COMMIT'Lİ (`86b355a`, qa+kontrol ONAY 1 tur).** "devam et" → play-test/karara takılı olmayan en yüksek-değerli açık P0. `Truck.HandleDeliveryServerRpc` `[ServerRpc(RequireOwnership=false)]`+public idi → client kutu taşımadan `requiredCargo` kez çağırıp bedava ödül+prestij topluyordu. **Kilit gerçek:** tek meşru çağıran `TruckTrigger` zaten server-only (`CanProcessDelivery`→`IsServer`), G16'da doğrulanmış model (server kutu collision'ını görür) → ServerRpc redundant + tek exploit yüzeyi. **Fix:** RPC → server-only düz metot `ProcessDelivery` (+`if(!IsServer) return` guard), TruckTrigger düz çağırıyor, despawn/consume + ödül/prestij değişmedi. TutorialTruck (ayrı sınıf) dokunulmadı. Headless 0 CS. Seçici commit (hassas font/SteamManager/ProjectSettings dosyaları staging dışı). **SIRADAKI açık P0/P1 (play-test'siz güvenli): N3 break-room client-auth (büyük), G6/G7 envanter-RPC sender==owner, G1-b ModifyMoney yeniden-değerlendir (G16 sonrası kapanabilir mi).** Kapsamlı yol haritası artifact'i de bu oturumda çıkarıldı.
- **Kablolama-boşluğu araştırması BİTTİ (qa salt-okunur) → PARK (kullanıcı kararı).** Şüphe doğrulandı: `ShelfController`/`TableController` (`Assets/NewCss/TableScripts/`) `SetLevel`/`levels[]` (3D-model swap) API'sinin **hiçbir üretim çağıranı yok** (yalnız dosya-içi + editor ContextMenu). Component'ler `The Main Office.unity`'de attach ama runtime hep level-0'da donuk. `UpgradePanel._visualUpgradeLevels` **ayrı** canlı roguelite perk-UI (eksik uç değil); gerçek kapasite `ShelfState.cs`. **Sonuç: (b) kısmen kopuk — çökme yok, sadece yükseltme 3D görsel geri bildirimi ölü, zararsız.** Kod DEĞİŞMEDİ. Kullanıcı PARK dedi → follow-up kapandı. İleride: (A) sil (git rm + sahne temizliği manuel) veya (B) perk visual-level → SetLevel yeniden-kabla. Detay: `release-push.md` C5 follow-up.
- **🔖 OTURUM KAPANDI (save).** Bu oturum: **12 commit / 6 iş, hepsi kontrol ONAY (1'er tur).** FAZ 1: **G10** (event geç-spawn wiring `40d1f78`) + **G16** (kutu-düşme server-auth `43bbcda`) → event sistemi netcode tamam. FAZ 2 (kullanıcı %80 bütçeyi buraya yatırdı): **ekonomi denetim raporu** (`plans/economy-audit-2026-07-13.md`, 16-gün sim 1P-4P sağlıklı, iflas sarmalı yok) + **GDD 5-değer doc-sync** (`432e813`) + **C5 wealthTax kaldırma** (`9d2c3b0`) + **Yol B ölü-kod silme** (`479dbf0`). Hassas çalışma-ağacı dosyalarına (8 font + SteamManager + LateJoinGuard + ProjectSettings) HİÇ dokunulmadı — hâlâ commit'siz, Steam testine bağlı. **SIRADAKI önerilen iş: kablolama-boşluğu araştırması** (Shelf/TableController seviye-API'sinin canlı çağıranı yok → raf/masa görsel-seviye oyunda kopuk olabilir; ShelfState vs ShelfController) — Play-test'e takılı DEĞİL, güvenli. **Play-test'e takılı bekleyenler:** Shelf/Table görsel init teyidi (bu oturumun silme borcu), C1 kota-ölümü kalibrasyonu (kutu/dk ölçümü), FAZ 0 merge. Detay: `plans/release-push.md` + devam.md alt girişler.

- **Yol B ölü-kod SİLİNDİ COMMIT'Lİ (`479dbf0`, qa+kontrol ONAY 1 tur, kullanıcı "tam temizlik" onayı).** C5 sonrası follow-up. qa salt-okunur trace: `UpgradeManager.Instance` HİÇBİR sahne/prefab'da yok → hep null → `OnUpgradePurchased` hiç fire etmez, `Buy()` çağıranı yok → tümüyle inert. **Atomik silme:** `UpgradeManager.cs`/`UpgradeAssets.cs`/`ItemType.cs` (+3 .meta) `git rm`. **Shelf/TableController sahnede CANLI** → silinmedi, sadece ItemType/UpgradeManager-bağlı ölü hunk'lar temizlendi (subscribe/HandleUpgradePurchased/CapacityUpgradeMapping/GetNextUpgradeItemType), canlı görsel-seviye kodu (SetLevel/UpdateVisual/levels[]/Start) korundu, kullanılmayan using kaldırıldı. Zero-regression, headless 0 CS, kalan-referans grep 0. **Play-test borcu (kullanıcıda): Shelf/Table görsel init.** **YENİ açık follow-up (kablolama-boşluğu): Shelf/TableController seviye-API'sinin canlı çağıranı yok → raf/masa görsel-seviye oyunda kopuk olabilir (gerçek kapasite muhtemelen ShelfState). Ayrı incele.**

- **C5 wealthTax KALDIRILDI COMMIT'Lİ (`9d2c3b0`, kontrol ONAY 1 tur).** Kullanıcı Seçenek A (kaldır) dedi. Terim tamamen çıkarıldı: `GameEconomySettings.wealthTaxRate` alanı + `CalculateRent` 3.→2 param, `DayCycleManager.GetTotalUpgradeValue()` metodu+çağrıları + fallback inline wealthTax. Zero-regression (zaten hep 0). Kira = `BaseRent[P]×rentGrowth^cycle×rentScaledMultiplier`. Simülasyon metodundaki yerel `totalUpgradeValue` (interactables/storeLevel/log) korundu. Yorum/tooltip stale ref'ler temizlendi. Headless 0 CS. **Follow-up (açık, ayrı iş): `UpgradeManager`/`ItemType`/`UpgradeAssets` artık ölü-kod mu?** EkonomiAyarlari.asset'teki orphan `wealthTaxRate:0.1` zararsız (Unity yok sayar).

- **FAZ 2 EKONOMİ DENETİMİ yapıldı (economist derin analiz, ~138K token) → rapor `plans/economy-audit-2026-07-13.md`.** Kullanıcı %80 kullanım hakkını buna yatırdı. **Sonuç:** Faz-1 değerleri (500 start / 1.15 rentGrowth / 40 penalty / 15 prestij / -1.5 ceza) kodda+asset'te canlı ve doğru; 1P/2P/3P/4P normalde 16 günü sağlıklı bitiriyor, death-spiral yok. **3 kritik bulgu:** (1) **C1 kota-ölümü = AÇMA** — kota formülü gerçek oyuncu kutu/dk verimine bağlı, Play-test'te ölçülmeden `_difficultyRatio` kalibre edilemez → PARK (Play-test blocker). (2) **C5 wealthTax = YENİ KÖK NEDEN:** `GetTotalUpgradeValue()` orphan (Yol B) sistemi okuyor → rate ne olursa 0; economist önerisi **Seçenek A (kaldır, zero-regression)**, alt B (UpgradePanel'e TotalSpentTL ekle + rate 0.03). **KULLANICI KARARI BEKLİYOR (A vs B).** (3) prestij tavanı (100) 3-4P'de gün 9-13 erken doluyor — pacing notu, kapsam dışı. **GDD doc-sync YAPILDI+COMMIT'Lİ** (müdür, KÜÇÜK iş): §4-§8 5 stale değer (rentGrowth 1.3→1.15, penalty 60→40, prestij 5→15, ceza -2→-1.5, start 100k→500) + wealthTax etkisizlik notu. **Sırada: kullanıcı C5 A/B seçince gameplay+kontrol ile implement.**

- **G16 kutu-düşme cezası server-auth COMMIT'Lİ (`43bbcda`, kontrol ONAY 1 tur).** `BoxFallPenalty.ApplyPenalty` client'tan çalışınca `SpendMoney`/`ModifyPrestige` server'a ServerRpc gönderiyordu → collision birden çok peer'de tetiklenirse ceza client-sayısı kadar. Fix: `ApplyPenalty` başına `NetworkManager.Singleton.IsServer` guard. **`NetworkBehaviour`'a ÇEVİRMEDİM** — script'i taşıyan prefablar (ithappy prop'ları: cup/plate/toy/cubes/Object) NetworkObject içermiyor, dönüşüm kırardı. Ses (`PlayDropSound`) guard dışında (her peer yerel çalar). Fırlatma zaten server-auth (`NetworkWorldItem.SetThrowForce` server-only) → server collision'ı görür, ceza kaybolmaz. Not: NetworkManager null iken ceza atlanır — oyun NGO co-op olduğundan pratik risk yok. Headless 0 CS. **Kalan latent P1/P2: G13/G19 ✅ zaten kapalı; asıl kalan büyük işler G9-artış2 + FAZ 0 merge.**

- **G10 event geç-spawn wiring COMMIT'Lİ (`40d1f78`, kontrol ONAY 1 tur).** Event başladıktan SONRA spawn olan truck/customer aktif event çarpanını almıyordu (`ApplyEventEffectToNewObject` yazılı ama çağrısız → etki gün içinde sönüyordu). **KRİTİK model teyidi:** event efektleri `OnActiveEventChanged` NetworkVariable-callback ile HER PEER'de yerel uygulanıyor (server-only DEĞİL), bu yüzden düzeltme de her-peer'de çalışan `OnNetworkSpawn`'a kondu — release-push'un "server spawner'a ekle" önerisi yanlış olurdu (sadece server kopyasını düzeltir). `EventEffectManager` static `Instance` + Truck/CustomerAI `OnNetworkSpawn`'dan `Instance?.ApplyEventEffectToNewObject`. Restore dict'e eklendiğinden `RemoveAllEventEffects` geç-spawn'ı da kapsıyor; çift-uygulama yok (kesişim yok). Headless derleme 0 CS. Cila-notu (blocker değil): Instance null iken uyarı-log'u yok. **Event sistemi (G9-artış1/G10/G11/G12) netcode olarak tamam; kalan G9-artış2 = yeni mekanik/plumbing (release-push.md).** Font atlası artefaktları commit-dışı bırakıldı.

## 2026-07-12
- **🔖 OTURUM KAPANDI (save):** Tam kayıt → `plans/oturum-log-2026-07-12.md`. Bu oturumda 14 commit / 21 bug kapandı (FAZ 1 otonom bug-avı) + 28-item envanter + roguelite merge-onayı. **Sıradaki önerilen iş: G10** (event `ApplyEventEffectToNewObject` wiring — güvenli, event sistemini bitirir). Detay ve tüm kalan-iş haritası log dosyasında. Çalışma-ağacındaki hassas dosyalara dokunulmadı (Steam testine bağlı).
- **G11+G12 event-senkron COMMIT'Lİ (`110a1d7`, kontrol ONAY — G12 tur-1, G11 tur-2).** G12: event oyuncu-efektleri `GetOwnedPlayer()` (IsOwner) ile her peer kendi oyuncusuna → tüm oyuncular etkilenir (eski `FindObjectOfType` tek rastgele oyuncuya uyguluyordu). G11: takvim server-seeded deterministik (`_calendarSeed`+`_calendarBaseDay` NetworkVariable, `System.Random`, üretim OnNetworkSpawn'da seed-driven) → tüm client'lar aynı takvimi görür. **Tur-1 kontrol KRİTİK yakaladı:** late-join'de mutable `startDay` sapması → base-day de senkronlandı (tur-2 ONAY). Event sistemi netcode olarak tamamlandı (kalan G9 artış-2 mekanik-event'ler ayrı). Kullanıcı "önerini yap" dedi, G11/G12 seçildi.
- **Küçük temizlik batch COMMIT'Lİ (`1ea6f32`, kontrol ONAY 1 tur):** G13 (VIP RNG `IsServer` guard — peer-divergence), G26 (ItemData `Resources.LoadAll` → build-once static cache, 3 site), G27 (`TimeBasedLightController.cs`→`AutoLightController.cs` rename, GUID korundu), G19 (lock-cleanup static'i `OnServerStopped`'a bağlandı — OnDestroy tuzağı atlandı). Kullanıcı riskli G9-artış2 yerine bu güvenli batch'i istedi. Hepsi P2/izole, ekonomi/auth dokunuşu yok.
- **Roguelite MERGE-ÖNCESİ regresyon taraması BİTTİ (qa, salt-okunur) → MERGE-BLOCKER YOK.** 6 bilinen risk regresyonsuz doğrulandı (pozisyonel-index/reconnect-replay/gambler↔all_in dışlama/server-auth/bulk_buy×reroll/tier-kilidi). Tek P2 (`UpgradePanel.cs:1091` `DayCycleManager.Instance` null-guard eksik, satın-alma RPC'de) **FIX'Lİ+commit'li** (merge-hardening, davranış değişmedi). Detay `release-push.md`. **Sonuç: dal kod olarak merge'e hazır** — kalan tek şey senin çok-oyunculu Play doğrulaması (FAZ 0) + merge kararı. Kullanıcı riskli ekonomik-plumbing (G9 artış-2/SURPRISE AUDIT) yerine bu güvenli taramayı seçti.
- **G9 (event sistemi) başladı — Artış-1 COMMIT'Lİ (`f4f07b6`, kontrol ONAY).** Takvimde gösterilip etkisiz kalan 7 event'ten 3'ü (çarpan-mappable) bağlandı: BUSY DAY (INTENSIVE DAY→rename, dailyCustomer 1.5→1.3), RAINY DAY (0.8), MARKETING DAY (1.2+reward0.7). Kök neden: `OnNewDayHandler` `eventNames.IndexOf(name)` bulamayınca -1=etkisiz. economist değerleri kullanıldı. Kapsam **11→14/17**. **Artış-2 (kalan 4: QUOTA/SURPRISE AUDIT/CUSTOMER SUPPORT/FESTIVAL) yeni mekanik gerektiriyor — economist değerleri + plumbing planı release-push.md G9'da HAZIR.** Not: `PhoneCallManager.SetCallChance`/`SetCustomerSupportActive` boş stub, DifficultyManager onları zaten çağırıyor (phone-chance aslında hiç implement değil — CUSTOMER SUPPORT ile birlikte doldurulacak). Artış-2 ekonomik-penalty plumbing = Play-test'te doğrulanmalı.
- **FAZ 1 düzeltme 5 COMMIT, 11 bug, hepsi kontrol ONAY (1'er tur), seçici commit.** Yukarıdaki 3 batch (`8a74238`/`1374a62`/`60e767b`) + **`9bc4141` G1-a** (SetMoney/ResetMoney server-only, `SetMoney(999999999)` arbitrary-set exploiti kapandı; çağıranlar server-side doğrulandı) + **`f9a3f1b` G3** (telefon anti-spam server-authoritative, per-client `ServerCallState` dict → RPC-spam bedava-para/timeSkip loop'u kapandı, `DayCycleManager.CurrentHour` client ile aynı kaynak). **Sıralama kullanıcı bana bıraktı** → Play-test'siz güvenli + yüksek-değer olanları önceliklendirdim. **Kalan FAZ 1:** Play-test'e bağlı G1-b+G16 (BoxFallPenalty server-auth — kutu-ownership fizik modeli test'te doğrulanmalı) & G2 (truck teslim-kanıtı); economist gerekli G9 (7 event multiplier)+G10/11/12; design teyidi G15/G23/G24/G25; dikkatli caller-analiz G6/G7 (RPC sender==owner, table/shelf legit çağrıları kırmadan); latent G13/G19/G26/G27/G28.
- **FAZ 1 düzeltme 3 BATCH COMMIT'Lİ, hepsi kontrol ONAY (1'er tur), seçici commit (hassas late-join/font/ProjectSettings dosyalarına dokunulmadı):** Batch-1 `8a74238` G8+G18+G5 (CustomerManager: spawn init sırası→perk+loot geri geldi, singleton guard, ölü RPC sil). Batch-2 `1374a62` G4+G17+G22 (EventEffectManager test-RPC sil, TruckTrigger NetworkObject.Despawn, Truck reward negatif-clamp). Batch-3 `60e767b` G14+G21+G20 (stamina writePerm=Owner, MoneyUI çift-subscribe, WaitBar despawn unsubscribe). **9 bug kapandı.** **Kalan FAZ 1 (release-push.md):** economist gerektiren G9 (7 event etkisiz→multiplier tanımı); karar/design G15 (movement client-auth kabul mü?), G23 (haksız prestij cezası isim/mantık), G24 (ESC global pause kastı?), G25 (StaminaBarUI ölü mü sil?); dikkatli-analiz G6/G7 (RPC sender==owner — legit table/shelf çağrılarını kırmadan), G16 (BoxFallPenalty IsServer — prefab fizik-replikasyon modeli doğrulanmalı); büyük/sistemik G1/G2/G3 (para/teslim/phone RPC auth katmanı — merge öncesi karar); latent G13/G19/G26/G27/G28.
- **FAZ 1 düzeltme Batch-1 COMMIT'Lİ (`8a74238`):** G8+G18+G5, tek dosya `CustomerManager.cs`. G8=spawn init sırası (SetupCustomerAI Spawn öncesine → perk+deterministic-loot geri geldi), G18=Start singleton guard, G5=ölü/açık `RequestCustomerSpawnServerRpc` silindi. gameplay yazdı → müdür diff doğruladı → **kontrol ONAY (1 tur)** → seçici commit (hassas late-join/font dosyalarına dokunulmadı). **Sırada Batch-2 önerisi:** G4 (EventEffectManager TestEventServerRpc dev-guard/sil, P0 ucuz) + G17 (TruckTrigger Despawn) + G22 (Truck reward clamp). Karar/economist gerektirenler ayrı: G9 (7 event etkisiz — multiplier tanımı=economist), G15/G24/G23/G25 (design teyit), G1-teması (sistemik RPC-auth, büyük).
- **FAZ 1 GENİŞ tüm-oyun taraması bitti (4 paralel qa, otonom oturum).** 28 bulgu G1-G28, triyajlı → `plans/release-push.md`. **Kod DEĞİŞTİRİLMEDİ (hiçbiri onaylı değil).** **Baskın tema:** `RequireOwnership=false`+doğrulamasız RPC sistemik (G1 MoneySystem sınırsız para — İKİ ajan doğruladı; G2 truck bedava ödül, G3 phone spam, G4 test-event). **En kritik non-exploit: G8** — CustomerAI init sırası bozuk → Sabırlı-Müşteriler perki + deterministic loot HİÇ çalışmıyor (ucuz reorder fix). G14 stamina writePerm spam. economist memo (C1 kota-ölümü→2-kademeli tampon öner, C5 wealthTax→kaldır öner) release-push FAZ 2'ye yazıldı. **ProjectSettings.asset açık sorusu KAPANDI:** IL2CPP/SENTIS değil, sadece LF→CRLF churn → commit öncesi `git checkout` yeter. Çalışma ağacına dokunulmadı (late-join fix testi bekliyor).
- **FAZ 1 çekirdek döngü + ekonomi taraması** (qa korrektlik) → 5 bulgu (E1-E5, `plans/release-push.md`). **E1/C2 (iflas TriggerLose spam), E2/C3 (replay kirli-state), E4/C4 (prestij≤0 merkezi game-over)** düzeltildi → kontrol ONAY (C2 tur-1 FAIL: `_networkIsDayOver` yan-etkisi yakalandı; ayrı `_gameOverStopProcessing` flag'iyle tur-2 ONAY) → derleme temiz → **commit'li.** **İzin modu artık `auto`** (settings.local.json). **KARAR bekleyen (FAZ 2/economist):** E3/C1 kota-tutturamama→game-over ölü kod (GDD'de var, bağlı değil), E5/C5 rent `wealthTax` hep 0 (`UpgradeManager.Buy()` hiç çağrılmıyor). E4-kalan yol yok; sırada C1/C5 için economist ya da yeni dilim.
- **FAZ 1 netcode başladı (kullanıcı testi atladı, paralel).** qa A+B taraması → 10 bulgu, triyajlı (`plans/release-push.md` N1-N10). Kullanıcı **N1+N2+N4** batch'ini onayladı → gameplay düzeltti → batchmode derleme temiz → **kontrol ONAY (1 tur)** → **commit'lendi.** N1: `PlayerSpawner` spawn ordering (NRE/DivByZero+orphan). N2: `LobbySaver` alt-tab/focus/pause artık lobiden çıkmıyor (sadece quit/destroy). N4: `NextDayServerRpc` guard (gün-over+breakReady şart, kira-atlama exploit'i kapandı). N2 diff'i mojibake churn'den arındırıldı (Latin-1 byte-safe re-apply). **Kalan bulgular açık:** P0 N3 (break-room client-auth, büyük iş), P1 N5+N6 (ikisi de SteamManager'a dokunuyor → **late-join finalizasyonuna** ertelendi, atlanmış multiplayer testine bağlı), P2 N8-N10. **Not:** kullanıcı rutin kapsam onaylarını sormamamı istedi → [[autonomous-proceed]] + `.claude/settings.local.json` allowlist geniş desenlere çevrildi (acceptEdits mode).
- **N7 de commit'lendi** (ayrı commit): `DayCycleManager.GetPlayerCount` kira için roster'ı otorite aldı (fallback ConnectedClients→1), kontrol ONAY, derleme temiz.
- **RELEASE PUSH başladı.** Kullanıcı hedefi: buglar → denge/sistemler → çıkış. **FAZ 0 (havadaki işi kapat) seçildi:** (1) late-join reddedilme UX'i, (2) reconnect'i in-game doğrula, (3) [NETDBG] temizle, (4) roguelite dalını main'e merge. Sonra FAZ 1 (tüm-oyun bug envanteri) → FAZ 2 (ekonomi + GDD sistem-boşluk) → FAZ 3 (Steam çıkış). Detay bu logda.
- **Multiplayer investigation (COMMIT'SİZ, çalışma ağacında):** 
  - **Lobi bug'ları ÇÖZÜLDÜ** — kök neden: Steam deposundaki **eski build** (roster senkron fix'leri yoktu). Depo `contentroot` yanlış klasörü (`content` vs `content\Build`) gösteriyordu → hep eski build yükleniyordu. `depot_3797161.vdf` düzeltildi (D:\...\ContentBuilder), güncel dal build'i Steam'e gidince lobi düzeldi. **App 3797160 / Depot 3797161**; `setlive` boş → partner sitesinde manuel Set Build Live gerekiyor.
  - **`[NETDBG]` teşhis enstrümantasyonu** SteamManager+LateJoinGuard'a eklendi (NGO LogLevel=Developer + ApproveConnection log + client DisconnectReason). Player.log: `C:\Users\cicek\AppData\LocalLow\Eclion Software\Cargor\Player.log`. **TEST SONRASI KALDIRILACAK** (bkz [[unity-batchmode-artifacts]] mantığı — geçici).
  - **Late-join reddedilme UX fix** (SteamManager.cs) — **kontrol ONAY (2 tur).** Whitelist-dışı client maç sonrası NGO'da reddedilince mesaj + `LeaveLobby()`. **1. tur DÜZELTME GEREKLİ:** `DisconnectReason` ile normal-leave ayrımı YANLIŞTI (NGO her Shutdown'da onu generic string ile doldurur → her sıradan çıkışta yanlış mesaj = regresyon). **Düzeltme:** ayrım artık `_clientConnectedThisSession` sinyaline dayanıyor (red = client hiç `OnClientConnected` almadan düşer). `LeaveLobby` bir sonraki frame'e ertelendi (re-entrant Shutdown önlendi). 2. tur ONAY.
- **⚠️ ÇALIŞMA AĞACI DURUMU (commit'siz):** `SteamManager.cs`+`LateJoinGuard.cs` = late-join fix **+** [NETDBG] enstrümantasyonu iç içe (ikisi de commit'siz). Ayrıca commit ETME: 8 Figma font + LiberationSans (Unity-açık artefaktı), `ProjectSettings.asset` (muhtemelen IL2CPP backend/SENTIS — [[unity-batchmode-artifacts]], incele), untracked `Assets/AddressableAssetsData/link.xml` + `Assets/_Recovery/` (crash recovery junk).
- **Sırada (FAZ 0 kalan) — DEVAM NOKTASI:** (1) kullanıcı **tek combined build** al (late-join fix + [NETDBG] dahil) → Steam'e yükle ([[cargor-steam-deploy]]) → **iki test:** A) late-join reddi (arkadaş maç sonrası katılamaz + net mesaj, VE normal çıkışta yanlış mesaj ÇIKMAMALI) B) reconnect (düş→bağlan→perk geri gelir). (2) İkisi ✅ → SteamManager+LateJoinGuard'ın **sadece late-join fix hunk'larını** commit'le (seçici; [NETDBG] hariç). (3) **[NETDBG] kaldır** (SteamManager 706-731 + client-disconnect log 795 + LateJoinGuard 128-131/146). (4) Font artefaktlarını revert. (5) **Roguelite dalını main'e merge.** Sonra FAZ 1 (tüm-oyun bug envanteri).
- **Yapıldı (`e80372c` COMMIT'Lİ):** **Task 9 (dal-sonu qa+düzeltme) — kontrol ONAY (1 tur).** qa taraması BÜYÜK bir gizli regresyon ortaya çıkardı: `HandleUpgradeLevelsChanged` `_entries[upgradeIndex]` pozisyonel erişimi kullanıyordu ama `_entries` sadece ≤3 draft kartı tutuyor → **perk etkileri çoğu index'te HİÇ uygulanmıyordu** (d8b33ff "3 kart" commit'inin regresyonu). Önceki "Task 7/8 Play ✅" yanlış pozitifmiş (kullanıcı sadece "hata yok" bakmış). **Fix:** `upgrades[index]`'ten hafif EntryUI + ortak `ReplayUpgradeLevel`. Ayrıca CRITICAL reconnect replay (`ReplayPurchasedUpgradeLevels`, idempotent), bulk_buy×reroll indirim temizliği, ölü `BuildEntries` kaldırıldı, magic number tek-kaynak, 2 DraftPool testi. **Runtime doğrulandı:** long_queue perki ertesi gün `CustomerManager.maxQueueSize`→5. Console temiz. Font atlas artefaktları commit dışı (Unity açık).
- **Sırada:** **Unity 1/2/4 kişi multiplayer testi** — özellikle **reconnect senaryosu** (CRITICAL replay'in asıl hedefi: bir oyuncu düşüp yeniden bağlanınca satın alınmış perkler client-local olarak geri gelmeli). Açık takip: EditMode Test Runner koşusu (opsiyonel, derleme zaten temiz), pool-limit köşe durumu notu. Roguelite dalı kod olarak **bitmiş** sayılır; kalan tek şey çok-oyunculu Play doğrulaması + main'e merge kararı.
- **Yapıldı (`7bdc5d9` COMMIT'Lİ):** **Task 7 authoring + Task 8 kapandı.** 16 perk sahnede (`The Main Office.unity`) — effectId/tier tabloyla birebir doğrulandı (headless), contentText'ler doğru Türkçe'yle değiştirildi. **Play-teyidi ✅ (kullanıcı, gözle hata yok)** — Task 8 tier kilidi (gün≥5 T2, gün≥9 T3) kodu zaten commit'liydi, Play'de teyit edildi. Commit'e WIP UpgradeUI png'leri + EkonomiAyarlari perk alanları da girdi. **Temizlik:** font atlas artefaktları (8 Figma+LiberationSans) revert; `EkonomiAyarlari.baseRentByPlayerCount` 500→5000 test kalıntısı revert. **Para notu:** başlangıç parası `DifficultyManager.baseStartingMoney` (Inspector), `MoneySystem.startingMoney` DEĞİL (ApplyMoneySettings eziyor).
- **Sırada:** **Task 9** — whole-branch qa taraması + ölü kod temizliği → **tüm-dal kontrol ONAY kapısı** → Unity 1/2/4 kişi multiplayer testi. (Task 7/8 artık bitti.) Ayrıca açık: geç-join relic re-apply Play-testi (`plans/manuel-gorevler.md`), pool-limit köşe durumu notu.
- **Yapıldı (`7110afc` COMMIT'Lİ):** Roguelite **Task 7 kodu** — `PerkEffect.cs` 16 perk + bulk_buy indirimi + high_volatility/emergency_brake + **Bulgu 2 dışlama** (gambler_case↔all_in: satın-alınca kalıcı düşme `BuildEligibility`+`IsBlockedByOwnedGroupMate`, satın-alma guard client+server, same-offer katmanı `DraftPool.SelectOffer` overload). gameplay yazdı, **qa+kontrol ONAY (2 tur)**. Değerler v3.2 birebir. **Takip (blocker değil):** late-join relic re-apply Play-testi (`plans/manuel-gorevler.md`), pool-limit köşe durumu (küçük not). Font artefaktları revert edildi; sahne/authoring commit dışı.
- **Sırada:** **Task 8** — Inspector perk verisi + tier kilidi (gün≥5 T2, gün≥9 T3), kullanıcının sahne authoring'ine (`plans/task7-manuel.md`: 16 perk + 9 backbone) bağlı. Authoring bitince tier davranışı Play'de teyit. Sonra Task 9 (qa + ölü kod) → whole-branch kontrol ONAY → Unity 1/2/4 kişi test.
- **Yapıldı (`f15e0c0` COMMIT'Lİ):** Netcode 3 fix tek commit'te — roster senkron + End Game host-only + reroll gate. Seçici commit: sadece ilgili hunk'lar (DayCycleManager roster hunk'ı, UpgradePanel reroll hunk'ı) awk+`git apply --cached` ile ayrıştırıldı, Task 7 kodu dışarıda bırakıldı, font artefaktları revert edildi. Reroll Play-teyidi kullanıcıdan ✅ geldi.
- **~~Bekliyordu~~ (ARŞİV):** Netcode 2 fix — (1) **Roster senkron**: server-authoritative `NetworkList<PlayerRosterEntry>` (yeni dosya `GameState/PlayerRosterEntry.cs`), Break Room / Daily Roster / Next Day UI artık roster'dan okuyor; host/client aynı oyuncuları görüyor. (2) **End Game host-only fix**: fire-and-forget ClientRpc → `NetworkVariable<GameEndState>` (None/Won/Lost) + `RenderRosterInto`; kazan/kaybet ekranı artık client'a da gidiyor. qa 6 bulgu (2 CRITICAL: FixedString surrogate truncation crash, ClearPlayerRoster ordering) → düzeltildi, kontrol ONAY. **Stage'de:** BreakRoomManager, NextDayUIManager, GameStateManager, PlayerRosterEntry(+meta), DayCycleManager(sadece roster hunk'ı). Seçici commit stratejisi — Task 7 kodu aynı dosyalarda karışık, awk+`git apply --cached` ile ayrıştırıldı.
- **Reroll bug ÇÖZÜLDÜ (kod, Play-teyidi bekliyor — commit'e KATILMADI):** `[RerollDBG]` log'ları kök nedeni verdi: `RerollServerRpc EARLY-RETURN: hour 7 < PANEL_OPEN_HOUR 10`. Panel OfficeTerminal üzerinden PANEL_OPEN_HOUR'dan önce (saat 7) açılabiliyor; `PurchaseUpgradeServerRpc`'de saat gate'i YOK (o yüzden satın alma saat 7'de çalışıyordu) ama reroll'a eklenen `hour<10` gate onu öldürüyordu. **Düzeltme:** reroll purchase ile simetrik — saat gate'i tamamen kaldırıldı, sadece para/geçerlilik kontrolü kaldı; debug log'lar temizlendi (`UpgradePanel.cs` OnReroll + RerollServerRpc). **Bekleyen:** kullanıcı Unity recompile + reroll'a bas → para düşüyor + 3 kart değişiyor mu teyit. UpgradePanel.cs stage'DE DEĞİL, teyit sonrası commit'e girecek. Not: gerçek server-auth panel-open state (OfficeTerminal↔UpgradePanel birleştirme) hâlâ ayrı iş olarak açık.
- **Task 7 durumu:** Kod (`PerkEffect.cs` 16 perk + CustomerAI/CustomerManager/GameEconomySettings/Truck/DayCycleManager Task7 hunk'ları) yazıldı, commit'siz. Kullanıcı Unity Editor'de manuel authoring yapıyor (`plans/task7-manuel.md`: 16 perk + 9 backbone sahne girişi, hepsi kind=Perk / requiresQuestSystem=false / levelObjects=boş). **Bulgu 2 kararı = (a) Dışlama** (gambler_case/all_in mutual exclusion — DraftPool'da müdür implement edecek, HENÜZ YAPILMADI). Bulgu 3 = economySettings doğrulama (kullanıcı).
- **Task 7 kontrol borcu:** PerkEffect emergency_brake relic-replay (geç-join resync riski), CalculateFinalCost imza değişikliği güvenlik incelemesi.
- **Artefakt temizliği borcu:** stage-dışı — 3 Figma font `.asset` + yeni `LiberationSans SDF - Fallback.asset` batchmode artefaktı → commit öncesi revert (bkz [[unity-batchmode-artifacts]]).

## 2026-07-09
- **Yapıldı:** Roguelite **Task 6** reroll butonu (`b458a9d`) · **Geç-join kilidi** (`e469c06` — oyun başlayınca whitelist-dışı red, orijinal Steam üyesi reconnect) → ikisi de kontrol ONAY. İş akışı optimize: koşullu kapı + subagent brief disiplini, kontrol modeli değişti (`4a52abf`).
- **Sırada:** Roguelite **Task 7** — 16-perk effect registry. ✅ **Hazır brief var: `plans/task7-prep.md`** (effectId→alan→değer eşlemesi + 5 gerçek kod dokunuşu; alan adları koddan doğrulandı, değerler v3.2'den). Sıfırlama sonrası bunu gameplay'e ver, soğuk türetme yok. Detay: `plans/roguelite-draft.md`.
- **Sende borç (manuel Play):** Geç-join (2+ Steam client) + reroll teyidi → `plans/manuel-gorevler.md`.
